# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Environments
.venv/
venv/
env/

# Build / Dist
build/
dist/
*.egg-info/

# Frontend
node_modules/
freemad_dashboard_ui/node_modules/

# IDEs / agent tooling. Skills are local configuration, not source: the canonical
# copy is .claude/skills/, and .agents/skills/ holds pointers to it (see AGENTS.md).
.idea/
.vscode/
.claude/
.agents/

# Local cache and logs
.mad_cache/
.benchmarks/
*.log
.env*

# Transcripts and artifacts
transcripts/*.json
transcripts/*.md

# Runtime state for the task and evolve runtimes: SQLite stores, per-iteration git
# worktrees, debate transcripts. Derived, per-machine, never source.
.freemad/

docs/plans/
.coverage
coverage.xml
sbom.spdx.json

# OS
.DS_Store
coverage.json
